#include<iostream>
#include<bits/stdc++.h>

using namespace std;

struct Node{
    int data;
    vector<Node *> childs;
};

struct Node * BuildKaryTree(int preorder[],int n,int k,int ind){

    return;
}

int main(){
    int no_of_vertices, no_of_queries;
    cin >> no_of_vertices;
    int no_of_edges = no_of_vertices - 1;
    cin >> no_of_queries;
    struct Node *root;
    for(int i=0; i< no_of_edges; i++){
        if(i==0){

        }
    }
    return 0;
}